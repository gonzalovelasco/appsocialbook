angular.module('app', ['bookList'])
.controller('indexCtrl', function($scope) {
  })/*.
  factory('Book', ['$resource',
    function($resource) {
        return $resource('books/:bookId.json', {}, {
        query: {
            method: 'GET',
            params: {bookId: 'books'},
            isArray: true
        }
        });
    }
])*/;
